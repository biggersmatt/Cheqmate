<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Cheqmate</title>
</head>
<body>
  <h1>Dashboard</h1>

  <header>
    <nav>
      <ul>
        <li>
          <a href="/">Login</a>
        </li>
        <li>
          <a href="/user/:id/ticket/new">Create New Ticket</a>
        </li>
        <li>
          <a href="/user/:id/ticket/:id">Ticket Show<%= ticket.title %> </a>
        </li>
      </ul>
    </nav>
  </header>

  <main>
    <h1>List of Tickets</h1>
    <ul>
        <% ticket.forEach((one) => { %>
            <li>
                <a href="/user/:id/ticket/<%= one._id %> "><%= one.title %></a> 
            </li>
        <% }) %>
    </ul>
</main>


</body>
</html>